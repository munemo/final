{

   "license" : "MIT",
  

  "engines": {
    "node": "16.2.0",
    "yarn": "1.22.5"
},
  "dependencies": {
    "body-parser": "^1.19.0",
    "cors": "^2.8.5",
    "dotenv": "^10.0.0",
    "express": "^4.17.1",
    "mongoose": "^5.12.11"
  },



  "scripts": {
    "start": "node Server.js", 
    "install-client": "cd client && yarn add client",
    "build-client": "cd client && yarn run build",
    "heroku-postbuild": "yarn run install-client && yarn run build-client"
}
}
